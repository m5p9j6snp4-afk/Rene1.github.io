<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Rene 的朋友圈</title>
    <style>
        body { 
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif; 
            background: linear-gradient(135deg, #764ba2 0%, #667eea 100%); 
            min-height: 100vh;
            margin: 0; 
            padding: 20px;
            box-sizing: border-box;
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        .container { 
            max-width: 450px; 
            width: 100%;
        }
        .header {
            text-align: center;
            margin: 20px 0 30px 0;
            color: white;
        }
        .title { font-size: 24px; font-weight: 800; }
        .subtitle { font-size: 14px; opacity: 0.8; margin-top: 5px; }

        /* ✨ 每一条动态的卡片样式 */
        .moment-card {
            background: rgba(255, 255, 255, 0.9);
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
            padding: 20px;
            border-radius: 18px;
            margin-bottom: 20px;
            box-shadow: 0 10px 25px rgba(0,0,0,0.1);
        }
        .moment-time {
            font-size: 12px;
            color: #a0aec0;
            font-weight: bold;
            margin-bottom: 8px;
            display: block;
        }
        .moment-content {
            font-size: 15px;
            color: #2d3748;
            line-height: 1.6;
        }
        /* 如果动态里有图片可以激活这个（目前先放个好玩的提示） */
        .moment-img {
            width: 100%;
            border-radius: 10px;
            margin-top: 10px;
            box-shadow: 0 4px 10px rgba(0,0,0,0.05);
        }

        /* 返回按钮 */
        .btn-back { 
            display: block; padding: 14px; background: #4a5568; color: white; 
            text-decoration: none; border-radius: 12px; font-weight: 600; font-size: 15px;
            text-align: center; box-shadow: 0 4px 12px rgba(74, 85, 104, 0.2); transition: all 0.2s ease;
            margin-top: 10px;
        }
        .btn-back:hover { background: #2d3748; transform: translateY(-2px); }
    </style>
</head>
<body>

    <div class="container">
        <div class="header">
            <div class="title">Rene's Moments 📸</div>
            <div class="subtitle">香港彭于晏的日常碎片记录</div>
        </div>

        <!-- 📌 朋友圈动态 2：新鲜出炉 -->
        <div class="moment-card">
            <span class="moment-time">2026年5月27日 22:40</span>
            <div class="moment-content">
                🎉 宣布个事！我的个人独立网站今天正式落成。有专属 BGM 歌单，还有绝密个人档案，欢迎各位父老乡亲常来踩踩！
            </div>
        </div>

        <!-- 📌 朋友圈动态 1：第一条 -->
        <div class="moment-card">
            <span class="moment-time">2026年5月25日 15:30</span>
            <div class="moment-content">
                有时候，一定要相信自己。听着卢广仲的歌，感觉世界都变温柔了。🎧✨
            </div>
        </div>

        <!-- 🏡 返回首页 -->
        <a href="index.html" class="btn-back">🏡 返回主页</a>
    </div>

</body>
</html>
